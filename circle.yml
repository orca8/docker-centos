machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build --rm -t local/c7-systemd-httpd .

test:
  override:
    - docker run -ti -v /sys/fs/cgroup:/sys/fs/cgroup:ro -v /tmp/$(mktemp -d):/run -p 80 local/c7-systemd-httpd
